<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:padding="10dp"
    android:layout_height="match_parent">



    <ImageView
        android:id="@+id/edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_mode_edit"

        android:paddingRight="10dp"
        android:layout_alignTop="@+id/amount_date"
        android:layout_toLeftOf="@+id/delete"
        android:layout_toStartOf="@+id/delete"
        android:layout_marginTop="7dp" />

    <ImageView
        android:id="@+id/delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_delete"
        android:layout_alignTop="@+id/edit"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />



    <TextView
        android:id="@+id/amount_view"
        style="@android:style/TextAppearance.Holo.Widget.TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text="iteme name"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:textSize="16dp"
        android:layout_toLeftOf="@+id/amount_date"
        android:layout_toStartOf="@+id/amount_date" />

    <TextView
        style="@android:style/TextAppearance.Holo.Widget.TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text="item price"
        android:id="@+id/amount_date"
        android:layout_alignBaseline="@+id/amount_view"
        android:layout_alignBottom="@+id/amount_view"
        android:layout_toStartOf="@+id/edit"
        android:layout_marginEnd="25dp"
        android:textSize="16dp"/>

</RelativeLayout>
